import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "AI Interview System",
  description: "AI-powered interview platform for seamless candidate evaluation",
  keywords: "AI interview, recruitment, candidate evaluation, automated interview",
  authors: [{ name: "Interview Agent" }],
  viewport: "width=device-width, initial-scale=1",
  robots: "noindex, nofollow", // Keep interviews private
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
